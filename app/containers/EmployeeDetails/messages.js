/*
 * EmployeeDetails Messages
 *
 * This contains all the text for the EmployeeDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EmployeeDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmployeeDetails container!',
  },
});
