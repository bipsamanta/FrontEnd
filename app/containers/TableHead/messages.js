/*
 * TableHead Messages
 *
 * This contains all the text for the TableHead container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableHead';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableHead container!',
  },
});
