/*
 * TableView Messages
 *
 * This contains all the text for the TableView container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TableView';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TableView container!',
  },
});
